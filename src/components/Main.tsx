import { IAnime } from "../entities/IAnime";

const Main = ({ animes }: { animes: IAnime[] }) => {
  return (
    <div className="mt-4 col-start-1 col-end-13 md:col-start-5 lg:col-start-4">
      Main
    </div>
  );
};

export default Main;
